c
(can? :cancel, @project) && (@project.can_cancel?)
(can? :cancel, @project) && true
(can? :cancel, @project)
can? :cancel && @project.can_cancel?
can? :cancel && @project.can_cancel?)
can? :cancel, && @project.can_cancel?)
can? :cancel, (&& @project.can_cancel?)
can? :cancel, (@project && @project.can_cancel?)
@project.can_cancel?
can? :cancel, @project
(can? :cancel, @project && @project.can_cancel?)
can? :cancel, @project && @project.can_cancel?
@project.can_cancel?
can? :cancel, @project
can? :cancel, @projec
can? :cancel, @project && @project.can_cancel?
c
can? :reject, @project && @project.can_reject?
can? :cancel, @project
@project.can_cancel?
c
